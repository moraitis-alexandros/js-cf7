/**
 * JavaScript uses a dynamic police, no datatypes needed
 * Variables are camelCase
 * JS is case-sensitive
 * Avoid declaring with var
 * take care of teh syntax. Spaces between operators/operands
 */

const PI = 3.14 //Declares 'true' constants
const maxThreads = 150 //Declares contexual variable (not change in our program context)
let counter = 0 //Declares non-constant
counter ++ 
console.log(PI, typeof PI, maxThreads, counter)